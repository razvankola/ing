[Unit]
Description= Spring PetClinic (JAR)
After=network.target

[Service]
Type= simple
WorkingDirectory=/opt/petclinic
ExecStart=/usr/bin/java -jar /opt/petclinic/app.jar
Restart=always
RestartSec=5
SuccessExitStatus=143

[Install]
WantedBy=multi-user.target
